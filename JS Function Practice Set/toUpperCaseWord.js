//↠  7. Define a function toUpperCaseWord that takes a string and returns it in uppercase.

function toUpperCaseWord(str) {
    return str.toUpperCase();
}
console.log(toUpperCaseWord("toUpperCase returns string in upper case")); // Output: "TOUPPERCASE RETURNS STRING IN UPPER CASE"